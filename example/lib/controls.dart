import 'bottom_menu.dart';

class cameraAction extends BottomNavigationOpenCameraFloatButton
{
  cameraAction(onTap) : super(onTap: onTap);
  // cameraAction({Key? key, required this.onTap, this.scanButton})
  //     : super(key: key);
}

class additionalFunctions extends BottomNavigationCircularNotchedAppBar
{
  additionalFunctions(onTap) : super(onTap: onTap);
}